<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoneyManagement">
        <activity
            android:name=".presentation.view.splashactivity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.view.addnewactivity.AddNewActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".presentation.view.transactionsactivity.TransactionsActivity"/>
        <activity android:name=".presentation.view.homeactivity.HomeActivity"/>
        <activity android:name=".presentation.view.staticactivity.StaticActivity"/>
        <activity android:name=".presentation.view.budgetactivity.BudgetActivity"/>
        <activity android:name=".presentation.view.budetdetailactivity.BudgetDetailActivity"/>
        <activity android:name=".presentation.view.jardetailactivity.JarDetailActivity"/>
        <activity android:name=".presentation.view.introactivity.IntroActivity"/>
    </application>

</manifest>